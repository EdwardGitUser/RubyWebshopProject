<!DOCTYPE html>
<html>
  <head>
    <title>Webshop</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
  <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">

<h5 class="my-0 mr-md-auto font-weight-bold"> Webshop </h5>

<span class="p-2 text-dark"><%= link_to "Головна сторінка",root_path%></span>
<span class="p-2 text-dark"><%= link_to "Producers",producers_path%></span>
<span class="p-2 text-dark"><%= link_to "Categories",categories_path%></span>
<span class="p-2 text-dark"><%= link_to "Items",items_path%></span>
<span class="p-2 text-dark"><%= link_to "About us", posts_path%></span>
</div>
<div class="container">
  
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>
<% if current_user %>
  <%= current_user.email %>
  <%= button_to "Log out", destroy_user_session_path, method: :delete, data: { turbo: false} %>
<% end %>
<%= yield %>
</div>

  </body>
</html>
